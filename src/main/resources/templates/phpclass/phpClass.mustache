<?php

class {{className}} {

{{#fields}}
    private {{type}} ${{name}};
{{/fields}}

    function __construct({{#fields}}{{type}} ${{name}}{{separator}}{{/fields}}) {
    {{#fields}}
        $this->{{name}} = ${{name}};
    {{/fields}}
    }

    {{#fields}}
    public function get{{capitalizedName}}(): {{type}} {
        return $this->{{name}};
    }

    public function set{{capitalizedName}}({{type}} ${{name}}): void {
        $this->{{name}} = ${{name}};
    }

    {{/fields}}
}
